services:
  lucky:
    image: gdy666/lucky
    restart: unless-stopped
    container_name: lucky
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - data:/goodluck
    logging:
        driver: "json-file"
        options:
            max-size: "1m"
    network_mode: host

volumes:
  data: